<h1>New image</h1>

<% form_for @image, :url => {:action => "create"},:html => { :multipart => true } do |f| %>
  <%= f.error_messages %>

  <%= f.file_field :image %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', schedule_images_path(params[:schedule_id]) %>